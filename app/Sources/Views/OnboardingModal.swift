import SwiftUI

// Kept as a compatibility view for the existing Xcode project reference.
// First-run onboarding now lives inline in `FirstRunStudio`, so users see one
// clear path instead of a modal stacked over another onboarding surface.
struct OnboardingModal: View {
    var body: some View {
        EmptyView()
    }
}
