# macOS
.DS_Store
*.swp

# Xcode / SwiftPM build artifacts
build/
.build/
DerivedData/
*.xcuserstate
xcuserdata/
.swiftpm/
Package.resolved

# Downloaded Core ML models (large; fetched on demand, not committed)
models/
*.mlpackage
*.mlmodelc

# Spike test media & generated indexes
spike/media/
*.sqlite
*.sqlite-*

# Secrets / local config
.env
*.local

# Unrelated project that lives in this working dir; never commit (contains secrets)
mcpmarket-me/

# XcodeGen-generated project (regenerate with `xcodegen generate`)
*.xcodeproj/

# Local agent/skill tooling (not part of the project)
.agents/
.claude/
skills-lock.json

# Release packaging output
dist/
